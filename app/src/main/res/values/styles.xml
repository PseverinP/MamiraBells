<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light"/>
        <!-- Customize your theme here. -->
    <!--<style name="SplashScreenTheme" parent="Base.Theme.AppCompat."/>-->


</resources>
